<form method="post" action="{{ path('app_exposition_delete', {'id': exposition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exposition.id) }}">
    <button class="btn">Delete</button>
</form>
